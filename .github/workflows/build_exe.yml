name: Build SchoolBell.exe

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install Pillow for icon
        run: pip install Pillow

      - name: Copy default sounds to data
        run: |
          if (!(Test-Path "data\sounds")) { New-Item -ItemType Directory -Path "data\sounds" -Force }
          Copy-Item "assets\sounds\*" "data\sounds\" -Force

      - name: Build EXE
        run: |
          pyinstaller --onefile --noconsole --name SchoolBell `
            --add-data "assets;assets" `
            --add-data "locales;locales" `
            --add-data "data;data" `
            --icon "assets/icon.ico" `
            --hidden-import "json" `
            --hidden-import "sqlite3" `
            --hidden-import "logging" `
            --hidden-import "traceback" `
            app/main.py

      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: SchoolBell
          path: dist/SchoolBell.exe
